{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "启动 VBI SQL Visualizer",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "构建扩展"
    }
  ]
}
