# spec パイプライン

## spec パイプライン

`spec pipeline` は advancedVSeed DSL を受け取り、spec を出力します。

`spec` は VChart または VTable の入力データ構造で、チャートの設定項目を記述するために使用します。

`spec` 自体は完全にはシリアライズできないため、Node.js 環境では構築できず、ブラウザー環境でのみ使用できます。
