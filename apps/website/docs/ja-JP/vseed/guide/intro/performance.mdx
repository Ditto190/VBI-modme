---
playgroundDirection: horizontal
---

import { Dataset20K } from '@components'

# 大規模データのパフォーマンス

VSeed は大規模データシナリオ向けに最適化されており、多数のデータポイントを滑らかにレンダリングできます。次の例では、20K データポイントのレンダリング性能を示します。

<Dataset20K />
