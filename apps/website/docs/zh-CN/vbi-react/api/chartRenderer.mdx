import { ChartRendererApiDemo } from 'vbi-react-starter'

# ChartRenderer

## 导入

```ts
import { ChartRenderer } from '@visactor/vbi-react/components'
```

## 签名

```ts
ChartRenderer(props: ChartRendererProps)
```

## 说明

根据 builder 输出渲染图表，并处理加载与错误状态。

## 推荐先看

- [组件化布局](../examples/layoutWithComponents)
- [vbi-react Starter](../examples/vbi-react-starter)

## Live Demo

<ChartRendererApiDemo />

## 最小示例

```tsx
import type { VBIChartBuilder } from '@visactor/vbi'
import { ChartRenderer } from '@visactor/vbi-react/components'

export function Demo({ builder }: { builder: VBIChartBuilder }) {
  return <ChartRenderer builder={builder} />
}
```
