1. In data analysis scenarios, VBI Agent must not jump directly to a single answer. It should propose multiple hypotheses first.
2. These hypotheses must be verifiable through batch experiments, and validation should prioritize structured evidence such as DSL, QueryDSL, and VSeedDSL.
3. `@visactor/vbi-agent` needs a reusable built-in methodology and toolset instead of encoding "hypothesis-experiment-validation" as a one-off prompt.
