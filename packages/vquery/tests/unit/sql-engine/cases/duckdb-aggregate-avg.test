# Upstream: duckdb/duckdb@7560f8256d70d36d8f0185222567939c48d78257
# Source: test/sql/aggregate/aggregates/test_avg.test
# License: MIT. Selected record blocks below are copied without changing setup SQL, query SQL, or expected results.

# scalar average
query RR
SELECT AVG(3), AVG(NULL)
----
3
NULL

statement ok
CREATE TABLE integers(i INTEGER);

statement ok
INSERT INTO integers VALUES (1), (2), (3)

query RRRR
SELECT AVG(i), AVG(1), AVG(DISTINCT i), AVG(NULL) FROM integers
----
2
1
2
NULL

query R
SELECT AVG(i) FROM integers WHERE i > 100
----
NULL
