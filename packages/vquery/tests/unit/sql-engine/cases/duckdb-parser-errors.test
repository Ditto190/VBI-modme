# Upstream: duckdb/duckdb@7560f8256d70d36d8f0185222567939c48d78257
# Source: test/sql/peg_parser/peg_syntax_error.test
# License: MIT. Selected record blocks below are copied without changing query SQL or expected errors.

statement error
SELECT * FROM tbl WHERE id IN (,);
----
Parser Error: syntax error at or near ","

statement error
SELECT a, b,, FROM tbl;
----
Parser Error: syntax error at or near ","

statement error
SELECT users..id FROM users;
----
Parser Error: syntax error at or near ".."

statement error
SELECT * FROM tbl WHERE x = (SELECT 1 FROM (SELECT 2)
----
Parser Error: syntax error at or near ")"

statement error
SELECT '2023-01-01'::
----
Parser Error: syntax error at or near "::"
